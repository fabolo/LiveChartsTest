<UserControl x:Class="WpfApp.EventIndicator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="60">
    <!--<Control.Resources>
        <local:Resources></local:Resources>
    </Control.Resources>-->
    <Canvas>
        <StackPanel x:Name="panel" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top">
            <Rectangle x:Name="arrow" Width="20" Height="20"/>
            <Border x:Name="border" CornerRadius="3"
                BorderBrush="Gray"
                Background="Beige"
                BorderThickness="1"
                Padding="1">
                <TextBlock x:Name="temperature" Text="999" Margin="10,2,10,2"></TextBlock>
            </Border>
        </StackPanel>
    </Canvas>
</UserControl>
